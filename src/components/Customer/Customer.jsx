import React from "react";
import styled from "styled-components";
import {Wrapper} from "../Wrapper";

const ListBook = styled.table``;

const Customer = () => {
  return <Wrapper></Wrapper>;
};

export default Customer;
